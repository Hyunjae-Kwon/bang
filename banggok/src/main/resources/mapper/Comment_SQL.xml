<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="comment">
	
	<!-- [selectTripComment] 여행 일정 공유 게시판 댓글 리스트 -->
	<select id="selectTripComment" parameterType="hashmap" resultType="hashmap">
	<![CDATA[
		SELECT * FROM BOARDCOMMENT
		WHERE BC_NUM = #{TR_NUM} AND BC_TYPE = 'T'
		ORDER BY BC_REGDATE DESC
	]]>
	</select>
	
	<!-- [tripComWrite] 여행 일정 공유 게시판 댓글 작성하기 -->
	<insert id="tripComWrite" parameterType="hashmap">
	<![CDATA[
		INSERT INTO BOARDCOMMENT (BC_BCID, BC_NUM, BC_ID, BC_REGDATE, BC_COMMENT, BC_TYPE)
		VALUES (BC_BCID_SEQ.NEXTVAL, #{BC_NUM}, #{BC_ID}, SYSDATE, #{BC_COMMENT}, 'T')
	]]>
	</insert>
	
	<!-- [comDelete] 댓글 삭제하기 -->
	<delete id="comDelete" parameterType="hashmap">
	<![CDATA[
		DELETE FROM BOARDCOMMENT
		WHERE BC_BCID = #{BC_BCID}
	]]>
	</delete>

</mapper>